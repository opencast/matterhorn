--- modules/matterhorn-confidence-monitoring-swf/src/main/flex/CaptureStatusRequest.mxml	(revision 0)
+++ modules/matterhorn-confidence-monitoring-swf/src/main/flex/CaptureStatusRequest.mxml	(working copy)
@@ -23,12 +23,12 @@
 	 		}
 	 		
 	 		private function onTick(event:TimerEvent):void{
-	 			Result.text = "onTick ";
+	 			//Result.text = "onTick ";
 	 			service.send();
 	 		}
 			
 			public function handleStateResult(event:ResultEvent):void{
-				Result.text += "Received Result";
+				//Result.text += "Received Result";
 				if(event.message.body == "idle" || event.message.body == "shutting_down" || event.message.body == "uploading" || event.message.body == "unknown")
 				{
 					tick++;
